{
  "name": "Verus Miner Static Builder",
  "image": "mcr.microsoft.com/devcontainers/universal:2",
  "features": {
    "ghcr.io/devcontainers/features/gcc:1": {},
    "ghcr.io/devcontainers/features/cmake:1": {},
    "ghcr.io/devcontainers/features/make:1": {},
    "ghcr.io/devcontainers/features/pkg-config:1": {}
  },
  "postCreateCommand": "apt-get update && apt-get install -y build-essential autoconf automake libtool pkg-config libcurl4-openssl-dev libjansson-dev libgomp1 && ./autogen.sh || true && ./configure --disable-opencl --disable-cuda --enable-static --with-pic LDFLAGS='-static' CFLAGS='-O3 -march=native' && make -j$(nproc)",
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-vscode.cpptools",
        "twxs.cmake"
      ]
    }
  },
  "remoteUser": "vscode"
}
